# Core
Pillow>=10.0.0
python-dotenv>=1.0.0

# OCR — Tesseract backend (install binary separately: https://github.com/tesseract-ocr/tesseract)
pytesseract>=0.3.10

# OCR — Azure AI Vision backend (OCR_BACKEND=azure)
azure-ai-vision-imageanalysis>=1.0.0
azure-core>=1.30.0

# Storage — Azure Blob Storage (STORAGE_BACKEND=azure)
azure-storage-blob>=12.19.0

# Database — PostgreSQL (DATABASE_URL set, hybrid/prod only)
psycopg2-binary>=2.9.0

# Testing
pytest>=7.4.0
